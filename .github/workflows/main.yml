name: CI

on: [push, pull_request, workflow_dispatch]

jobs:
  build-linux-gcc:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Setting up Environment
        uses: actions/setup-python@v2
        with:
          python-version: '3.x'

      - name: Install Dependencies
        run: |
          apt-get install gcc flex bison libglew-dev libsdl2-dev
          pip install meson ninja

      - name: Build
        run: |
          meson build
          ninja -C build homeworld tools assets
        env:
          CC: gcc

  build-linux-clang:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Setting up Environment
        uses: actions/setup-python@v2
        with:
          python-version: '3.x'

      - name: Install Dependencies
        run: |
          apt-get install clang flex bison libglew-dev libsdl2-dev
          pip install meson ninja

      - name: Build
        run: |
          meson build
          ninja -C build homeworld tools assets
        env:
          CC: clang

  build-macos-gcc:
    runs-on: macos-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Setting up Environment
        uses: actions/setup-python@v2
        with:
          python-version: '3.x'

      - name: Install Dependencies
        run: |
          brew install gcc flex bison glew sdl2
          pip install meson ninja

      - name: Build
        run: |
          meson build
          ninja -C build homeworld tools assets
        env:
          CC: gcc

  build-macos-clang:
    runs-on: macos-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Setting up Environment
        uses: actions/setup-python@v2
        with:
          python-version: '3.x'

      - name: Install Dependencies
        run: |
          brew install clang flex bison glew sdl2
          pip install meson ninja

      - name: Build
        run: |
          meson build
          ninja -C build homeworld tools assets
        env:
          CC: clang
